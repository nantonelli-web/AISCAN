/**
 * /api/brands/check-channel — alias of /api/competitors/check-channel
 * (Compare / Report per-channel availability probe). See ../route.ts for
 * the re-export rationale.
 */
export { GET } from "@/app/api/competitors/check-channel/route";
