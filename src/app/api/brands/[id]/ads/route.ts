/**
 * /api/brands/[id]/ads — alias of /api/competitors/[id]/ads (paginated
 * "load more" ads). See ../../route.ts for the re-export rationale.
 */
export { GET } from "@/app/api/competitors/[id]/ads/route";
